server {
    listen 80;
    server_name gaeilge.quest www.gaeilge.quest;

    # Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect everything else to HTTPS once cert is live
    # (uncomment after cert obtained)
    # return 301 https://$host$request_uri;

    # Proxy to gunicorn (comment out once HTTPS redirect is in place)
    location / {
        proxy_pass http://127.0.0.1:5200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 30;
    }
}

# HTTPS block — fill in after certbot runs
# server {
#     listen 443 ssl http2;
#     server_name gaeilge.quest www.gaeilge.quest;
#
#     ssl_certificate /etc/letsencrypt/live/gaeilge.quest/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/gaeilge.quest/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:5200;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_read_timeout 30;
#     }
# }
